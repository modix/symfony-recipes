name: Update Flex endpoint

on:
  push:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  call-flex-update:
    uses: modix/symfony-recipes/.github/workflows/callable-flex-update.yml@main